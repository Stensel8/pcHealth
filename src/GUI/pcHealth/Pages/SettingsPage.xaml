<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="pcHealth.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer Padding="28,20,28,24">
        <StackPanel MaxWidth="600" Spacing="16">

            <TextBlock Text="Settings"
                       Style="{StaticResource TitleTextBlockStyle}"/>

            <TextBlock Text="Appearance"
                       Style="{StaticResource BodyStrongTextBlockStyle}"
                       Margin="0,8,0,0"/>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8"
                    Padding="20,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Center" Spacing="2">
                        <TextBlock Text="App theme"
                                   Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Text="Light, dark, or follow system setting"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                    <ComboBox Grid.Column="1"
                              x:Name="ThemeCombo"
                              VerticalAlignment="Center"
                              SelectionChanged="ThemeCombo_SelectionChanged">
                        <ComboBoxItem Content="System default" Tag="Default"/>
                        <ComboBoxItem Content="Light" Tag="Light"/>
                        <ComboBoxItem Content="Dark" Tag="Dark"/>
                    </ComboBox>
                </Grid>
            </Border>

            <TextBlock Text="Behavior"
                       Style="{StaticResource BodyStrongTextBlockStyle}"
                       Margin="0,8,0,0"/>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8"
                    Padding="20,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Center" Spacing="2">
                        <TextBlock Text="Run scripts elevated"
                                   Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <TextBlock Text="Launch PowerShell scripts with administrator rights"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                    <ToggleSwitch Grid.Column="1"
                                  x:Name="ElevatedToggle"
                                  VerticalAlignment="Center"
                                  IsOn="True"
                                  OnContent="" OffContent=""
                                  Toggled="ElevatedToggle_Toggled"/>
                </Grid>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
